# Planning pipeline configuration
# Using OMPL as default planner - good general purpose choice for the D1-550

planning_plugin: ompl_interface/OMPLPlanner
request_adapters: >-
  default_planner_request_adapters/AddTimeOptimalParameterization
  default_planner_request_adapters/ResolveConstraintFrames
  default_planner_request_adapters/FixWorkspaceBounds
  default_planner_request_adapters/FixStartStateBounds
  default_planner_request_adapters/FixStartStateCollision
  default_planner_request_adapters/FixStartStatePathConstraints
start_state_max_bounds_error: 0.1

# OMPL planner settings
ompl:
  planning_plugin: ompl_interface/OMPLPlanner

  arm:
    default_planner_config: RRTConnect
    planner_configs:
      - RRTConnect    # Fast bidirectional planner - good default
      - RRT           # Standard RRT
      - PRM           # Probabilistic Roadmap - good for repeated tasks
      - LBKPIECE      # Good for high-DOF arms

  gripper:
    default_planner_config: RRTConnect
    planner_configs:
      - RRTConnect

planner_configs:
  RRTConnect:
    type: geometric::RRTConnect
    range: 0.0        # max step size (0 = auto)

  RRT:
    type: geometric::RRT
    range: 0.0
    goal_bias: 0.05

  PRM:
    type: geometric::PRM
    max_nearest_neighbors: 10

  LBKPIECE:
    type: geometric::LBKPIECE
    range: 0.0
    border_fraction: 0.9
    min_valid_path_fraction: 0.5
